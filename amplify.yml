version: 1
backend:
  phases:
    build:
      commands:
        - npm install  # <--- Forces direct installation on the cloud machine
frontend:
  phases:
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
cache:
  paths: []

